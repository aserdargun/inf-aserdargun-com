"use client";
import type { OwnerCatalogResponse, SettingsStatsResponse } from "@inf/contracts";
import { CalendarDays, Inbox, Library, RefreshCw, Sparkles } from "lucide-react";
import { useCallback, useEffect, useState, type ReactNode } from "react";
import { MediaFrame } from "../../components/ui/media-frame";
import { PageState, RetryButton } from "../../components/ui/page-state";
import { apiRequest } from "../../lib/api-client";
import { routes } from "../../lib/routes";
type TodayState = "loading" | "empty" | "error" | "success";
export function TodayPage() { const [state, setState] = useState<TodayState>("loading"); const [catalog, setCatalog] = useState<OwnerCatalogResponse | null>(null); const [stats, setStats] = useState<SettingsStatsResponse | null>(null); const load = useCallback(async () => { setState("loading"); try { const [nextCatalog, nextStats] = await Promise.all([apiRequest<OwnerCatalogResponse>("/api/infographics"), apiRequest<SettingsStatsResponse>("/api/settings/stats")]); setCatalog(nextCatalog); setStats(nextStats); setState(nextCatalog.infographics.length === 0 ? "empty" : "success"); } catch { setState("error"); } }, []); useEffect(() => { void load(); }, [load]); if (state === "loading") return <TodayHeading><PageState kind="loading" title="Loading Today…" /></TodayHeading>; if (state === "error") return <TodayHeading><PageState action={<RetryButton onRetry={() => void load()} />} kind="error" title="Today could not be loaded. Try again." /></TodayHeading>; if (state === "empty") return <TodayHeading><PageState action={<a className="button button--primary" href={routes.add}>Add infographic</a>} kind="empty" title="Nothing needs your attention right now." /></TodayHeading>; const recent = catalog!.infographics.slice(0, 4); const reviewNext = catalog!.infographics.filter((item) => item.reviewDueAt !== null).slice(0, 3); return <TodayHeading><section aria-label="Learning overview" className="status-row"><a href={routes.inbox}><Inbox aria-hidden="true" size={16} strokeWidth={1.75} />Inbox {stats!.inbox}</a><a href={routes.library}><Library aria-hidden="true" size={16} strokeWidth={1.75} />Library {stats!.library}</a><a href={routes.review}><CalendarDays aria-hidden="true" size={16} strokeWidth={1.75} />Due today {stats!.due}</a></section><section aria-labelledby="recent-title"><h2 id="recent-title">Recently added</h2><div className="media-rail">{recent.map((item) => <MediaFrame alt={item.title} caption={item.title} date={new Date(item.capturedAt).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })} key={item.id} src={`/api/public/images/${item.thumbnailDriveFileId}`} />)}</div></section><section aria-labelledby="review-next-title" className="review-next"><h2 id="review-next-title">Review next</h2>{reviewNext.length === 0 ? <p>No reviews are due right now.</p> : <ul>{reviewNext.map((item) => <li key={item.id}><span>{item.title}</span><span>Due today</span></li>)}</ul>}</section></TodayHeading>; }
function TodayHeading({ children }: { children: ReactNode }) { return <div className="today-page"><header><h1>Today</h1><p>Return to what matters.</p></header><div className="today-actions"><a className="button button--primary" href={routes.review}><RefreshCw aria-hidden="true" size={20} strokeWidth={1.75} />Start review</a><a className="button button--secondary" href={routes.surprise}><Sparkles aria-hidden="true" size={20} strokeWidth={1.75} />Surprise me</a></div>{children}</div>; }
