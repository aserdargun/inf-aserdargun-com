"use client";
import { useEffect, useState } from "react";
import { Button } from "../../components/ui/button";
export default function LoginPage() { const [state, setState] = useState<"default" | "loading" | "error">("default"); useEffect(() => { const query = new URLSearchParams(window.location.search); if (query.has("error")) setState("error"); else if (query.has("pending")) setState("loading"); }, []); function signIn() { setState("loading"); window.location.assign("/.auth/login/github?post_login_redirect_uri=/"); } return <main className="login-page"><section aria-labelledby="login-title"><p className="wordmark">INF</p><h1 id="login-title">Sign in</h1><p>This private notebook is available to its owner.</p>{state === "error" ? <p aria-live="polite" className="error-copy">We could not sign you in. Try again.</p> : null}<Button disabled={state === "loading"} onClick={signIn}>{state === "loading" ? "Signing in…" : "Continue with GitHub"}</Button>{state === "error" ? <Button onClick={signIn} variant="secondary">Try again</Button> : null}</section></main>; }
